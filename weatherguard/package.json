{
  "name": "weatherguard",
  "version": "1.0.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "cd admin && npm install && npm run build && cd .. && rm -rf dist && mv admin/dist dist"
  }
}
